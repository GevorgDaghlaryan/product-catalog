/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['fakestoreapi.com'], // Replace 'example.com' with your actual domain
    },
};

export default nextConfig;